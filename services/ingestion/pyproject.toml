[project]
name = "energy-ingestion"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Rafael-Creator-max", email = "pereztarirastalin@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
energy-ingestion = "energy_ingestion:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.5",
]
